# Backport: perfil_mensal_cvm 2026-06-28 lesson batch → templates

Branch: `feat/backport-cvm-2026-06-28-poetry-env-seams` · repo version bumped 0.11.8 → 0.12.0 (minor).
Plan: `~/.claude/plans/gleaming-purring-sedgewick.md`. Scope: `templates/python-common/` (all 5 Python tiers).

## Pre-work
- [x] Create branch off `main`
- [x] `make bump_version` (minor → 0.12.0)
- [x] Create this backlog file

## Lesson 1 — `make init` seeds `.env` (language-common)
- [x] New `bin/ensure_env.sh`
- [x] `Makefile`: `init` prereq (`-@`), `.PHONY`, target, help
- [x] `tasks.sh`: `ensure_env()`, `init()` (`ensure_env || true`), case, help

## Lesson 2 — recipes resolve Poetry, never bare (language-common)
- [x] New `bin/poetry_exec.sh`
- [x] `Makefile`: `POETRY` var + all `$(POETRY)` calls (incl. `cd src` mypy)
- [x] `tasks.sh`: `poetry_exec()` + all calls
- [x] `.pre-commit-config.yaml`: 6 `entry:` routed via `poetry_exec.sh`
- [x] `bin/db.sh`: `ensure_poetry` + `run_poetry`
- [x] `bin/lint_sql.sh` + `bin/lint_yaml.sh`: resolve-don't-install
- [x] Sweep: no bare `poetry` left (CI `tests.yaml` intentionally out of scope — runner PATH)

## Lesson 3 — Playwright best-effort + OS-aware (language-common)
- [x] `bin/venv.sh`: `try_install_playwright_browsers()` + rewire
- [x] `bin/fix_playwright.sh`: OS-aware `--with-deps` + `run_poetry`

## Lesson 4 — parameterized `bump_version` recipe (python-common)
- [x] `Makefile`: `LEVEL ?= patch` + `bump_version` (dropped `version_bump_minor`)
- [x] `tasks.sh`: `bump_version()` (dropped `version_bump_minor`)

## Lesson 5 — `precommit` handles non-git deploy tree (language-common)
- [x] New `bin/precommit.sh` (skip-gracefully default; git-init documented as opt-in)
- [x] `Makefile`: `precommit:` → delegate
- [x] `tasks.sh`: `precommit()` → delegate

## Lesson 6 — test the bash seams (language-common)
- [x] New `tests/integration/test_bin_scripts.py`
- [x] "Testing shell scripts" convention in `bin/CLAUDE.md` + both MVC `tests/CLAUDE.md`
- [x] Wired copy into 5 scaffold scripts + drop `.keep`
      (deviation: no separate `python-common/tests/CLAUDE.md` — would overwrite the
      per-tier one; convention lives in the universal `bin/CLAUDE.md` instead)

## Docs
- [x] `python-common/CLAUDE.md` files table + editing rules
- [x] `python-common/bin/CLAUDE.md` never-bare rule + rows + shell-test convention
- [x] `docs/blueprintx-lessons.md` backported note; global README index already current

## Verification
- [x] shellcheck + `bash -n` on all 8 new/edited bin scripts — clean
- [x] `make lint` at repo root — all green (shellcheck, codespell, version sync, mkdocs strict)
- [x] Scaffold MVC-native + DDD-native — all new files landed, `.keep` removed
- [x] Scaffolded project: integration test 3/3 pass; `ensure_env` seeds + no-ops;
      `bump_version` parity (make `LEVEL=patch` + tasks.sh positional `minor`); precommit
      skips off a git tree; pre-commit config valid + 6 entries routed
- [ ] `act` on scaffolded `tests.yaml` — DEFERRED: tests.yaml unchanged by this backport
      (CI uses runner-PATH poetry by design); heavy Docker+install for a throwaway demo.
      Changed CI-adjacent surface (pre-commit) validated above.

## Round 2 — batch grew to 9 lessons (added after round 1)
- [x] L7 `lint-resolve-vendored-cli-via-poetry-run` — `bin/lint_shell.sh` resolves
      shellcheck/shfmt via `poetry run` → system → skip (probe `--version`)
- [x] L8 `shellcheck-pip-installable-shellcheck-py` — added `shellcheck-py` to dev group
      (5 pyproject.toml) + new `bin/install_shell_linters.sh` + `install_shell_linters`
      recipe (Makefile + tasks.sh); dropped stale "shellcheck is a system binary" comment
- [x] L9 `merge-recipe-checkpoint-runs-full-gate` — **N/A to template**: the offline
      `git_merge_to_main.sh` has no `record_checkpoint`/`docs/checkpoints.md` step
      (that's a PG-only elaboration); nothing to warn about. Documented, not applied.
- [x] Consequence of L7: shfmt now actually runs → shfmt-formatted 14 latent
      space-indented shipping scripts (offline `common/bin/*`, `*/bin/lib/*`, run/db/etc.)
      so a fresh scaffold's `make lint` leaves the tree clean
- [x] Docs: bin/CLAUDE.md note; python-common/CLAUDE.md rows; blueprintx-lessons.md round-2
- [x] Verify: shellcheck + repo make lint green; act scaffold-lint-test mvc-native +
      lib-minimal both pass (shellcheck now runs via poetry, tree clean, tests pass)

## Round 3 — batch grew to 12 lessons
- [x] L11 `config-keyword-accept-all-spellings` — hardened the real existing keyword input
      `config/env_config.py` (the `ENV` selector): added `_normalise_keyword` (strip
      diacritics + lower + trim + fold spaces/hyphens to `_`) before the `ENV_TO_SUFFIX`
      lookup, so `produção`/`PROD`/`prd ` resolve instead of aborting; parametrised test +
      .env.example note in all 5 tiers (+ made test_env_config.py fully docstring-clean)
- [x] L12 `time-scoped-override-requires-reference-month` (mvc-*) — added as a 4th bullet in
      both MVC CLAUDE.md "Data-handling guardrails (advisory)" sections (matches its sibling
      mvc-* advisories already there); no override loader exists to add code to
- [x] L10 `contributor-workflow-highlighted-doc` — **N/A to template**: the nav-highlight
      seam (`nav-focus.js`, `op-focus`/`crisis-focus`) and the operator/crisis doc pages it
      adds a third colour to don't exist in any template; backporting = inventing PG-specific
      docs infrastructure, not extending an existing seam. Documented, not applied.
- [x] Docs: blueprintx-lessons.md round-3 note; global README index already current
- [x] Verify: env_config normaliser logic table; repo make lint green; act scaffold-lint-test
      mvc-native → 98 passed, tree clean, test_env_config warning-free

## Round 4 — 06-29 + 06-30 batches (4 new lessons)
- [x] L16 `decimal-coercion-nan-safe` (python-common) — `utils/decimals._parse` funnels
      Decimal/float/string paths through a new `_finite_or` (`.is_finite()`) so NaN/±Inf →
      default; parametrised NaN/Inf test added; reworded ERA001-tripping comments
- [x] L15 `join-keys-canonical-both-sides` (python-common) — 5th advisory bullet in both
      MVC CLAUDE.md "Data-handling guardrails" (canonicalise join keys both sides at read)
- [ ] L13 `multi-intent-pipeline-dispatch` (mvc-*) — BIG opt-in scaffold feature (user
      approved "build on this branch"): scaffold prompt (default No) + Yes-path pipeline
      trio (`pipeline_<intent>.py` + `pipeline_dispatch.py` + `pipeline_common.py`) +
      dispatching main + `PIPELINE_INTENT` resolver + always-shipped unified
      `controller/CLAUDE.md` (stamped `<!-- pipeline-mode: single|multi -->`); both MVC tiers
- [ ] L14 `pipeline-persist-payload-bounded-backfill` (mvc-*) — advisory inside L13's
      multi-mode `controller/CLAUDE.md` (reconcile-intent values-store + bounded backfill);
      project-specific machinery, documented not code-gen'd
- [x] L17 `ci-lock-fail-loud-not-regenerate` (language-common, blueprintx-origin) — validated
      bump_version never invalidates the lock (`poetry version` leaves content-hash untouched,
      empirically); CI `tests.yaml` now FAILS LOUD on a stale lock instead of `rm + poetry lock`
      (which silently upgraded every dep). New global lesson file + index + mirror.
- [x] Docs: blueprintx-lessons.md round-4 note; global README index
- [ ] Verify: ruff (done L16); act scaffold-lint-test both MVC tiers (after L13)

## Not committed
Branch + version bump done per request; commits made on explicit request (round 1: PR #36).
