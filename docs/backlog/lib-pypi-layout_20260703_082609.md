# Backlog: enhance `lib-minimal` into a PyPI-library skeleton

Branch: `feat/lib-pypi-layout` — plan: `~/.claude/plans/sunny-knitting-fern.md`

## Slices

- [x] **python-common `retry.py` — DI logging** (dropped `utils.logs` import; `LogEmitter`
      class w/ stdlib default; optional `cls_logger=None`). Unit test added; DI smoke-tested.
- [x] **python-common `zip_extractor.py` — in-memory extraction** (`extract_all_to_memory`,
      `extract_members_to_memory`, `extract_member_to_memory`) + unit tests. Smoke-tested.
- [x] **python-common `tests.yaml` — `workflow_call` output** — ALREADY present (no-op;
      identical to stpstone).
- [x] **Shared `ruff.toml`** — additive `_internal` per-file-ignore globs.
- [x] **Shared `mypy.ini`** — exclude `_internal/utils/typing/` + `[mypy-*._internal.utils.typing.*]`.
- [x] **New release workflows** `{release_pypi,release_test_pypi}.yaml` — parameterized,
      restricted-envsubst verified (`${{ }}` preserved, valid YAML).
- [x] **`bin/scaffold/python_lib_minimal.sh`** — package `src/<name>/`, private
      `_internal/{utils,config}`, import rewrite, docker-compose prompt, drop `.env`, render
      workflows, license-classifier map. shellcheck clean.
- [x] **`templates/lib-minimal/pyproject.toml`** — dropped python-dotenv, pandas+openpyxl core,
      twine+build dev, classifiers/keywords, `exclude` CLAUDE.md from wheel.
- [x] **`templates/lib-minimal/.coveragerc`** — `omit = src/*/_internal/*`.
- [x] **Docs**: rewrote `CLAUDE.md` (+ Architecture one-class-per-module hint), new
      `config_CLAUDE.md`, `docs/usage.md` publishing note, deleted `.env.example`, refreshed
      `skeleton.meta`.
- [x] **Verify**: scaffolded wwdates + pricing_derivatives → ruff/mypy clean, unit test pass,
      imports resolve, `poetry build` wheel contains `wwdates/_internal` + no top-level
      utils/config leak, docker-compose path, offline path. Repo CI checks (shell/meta/version/
      spelling) pass.
- [x] **Lessons**: `docs/blueprintx-lessons.md` + global store (in-memory zip; private `_internal`
      for distributables; restricted-envsubst for `${{ }}`; retry DI).
- [x] **Release**: `make bump_version` → 0.12.2. Commit + PR pending.

## Notes / decisions
- Keep `src/` (flatten deferred — would fork ~9 shared configs).
- `logs.py` is **opt-in** via a scaffold prompt (`prompt_logs`); retry.py uses injected
  `LogEmitter` regardless. `br_identifiers.py` shipped (CNPJ check).
- CODEOWNERS + PR template already shipped identical to stpstone — no change.
- Added `_internal/utils/CLAUDE.md`: logging → dependency injection convention (retry.py
  reference). Shipped always; excluded from the wheel.

## Follow-up fixes (post initial PR)
- [x] Backlog filename → kebab-case name part (was `lib_pypi_layout_…`, convention violation).
- [x] logs.py opt-in prompt + utils/CLAUDE.md (DI-for-logging convention).
- [x] **E501/I001 on long package names** — the `<pkg>._internal.` import-prefix rewrite
      overflowed 99 chars for longer names (e.g. `pricing_derivatives`). Root fix: wrap the
      `contracts/__init__.py` re-export with a trailing comma (pins multi-line for all tiers)
      + fully exclude the vendored typing engine from ruff (mirrors `src/chassis` + mypy).
      Re-verified `pricing_derivatives` (with logs): ruff + mypy clean, imports resolve.
