# Duskko — triagem completa das issues abertas antes de scaffoldar

**Criado:** 2026-08-16 11:00 · **Revisado:** 2026-08-16 12:10 (triagem rasa corrigida a pedido do dono)
**Base:** `v0.15.3` publicada (PR #170 mergeado) · **52 issues abertas**, todas classificadas abaixo

> **Revisão 2026-08-23 09:30 — o ledger estava atrasado.** Cruzando esta lista com
> `gh issue list --state open`, **13 caixas marcadas `[ ]` já estavam fechadas**: #128 #120
> #150 #119 #125 #122 #141 #126 #123 #146 #156 (seção A) e #173 #174 (seção B). Ticadas
> acima. ⚠️ **#113 continua ABERTA** — uma revisão anterior a deu como entregue e ela não é.
> Restam **16** itens de seção A: #116 #144 #148 #117 #127 #124 #152 #111 #145 #130 #113
> #118 #121 #151 #161 #162.

O duskko é um **`mvc-service-native-db`**: coleta na **API da Tesouraria** (JSON/HTTP,
substituindo as queries de `contexto_duskko/config/consultas_sql/`), tratamento, persistência em
**SQLite**. Ambientes: **Windows + proxy TLS da XP** *e* Linux/WSL. Um **segundo desenvolvedor**
(Werner) assume, num projeto **bilíngue** (docs PT-BR, código inglês).

Esses cinco fatos são o critério de corte de tudo que segue.

⚠️ **Scaffolding é cópia one-shot.** Correção que entrar depois do `make new` vira backfill manual
(#109). É por isso que a ordem importa.

---

## Já entregue na v0.15.3 — não repetir

| # | O que resolve |
|---|---|
| #158 | `default_stages` — todo gate rodava 2x (commit **e** push) |
| #143 | `tabular_reader`: arquivo só-cabeçalho não reprova; célula vazia não crasha no pandas 3 |
| #165 | índice 403 parcial não aborta o `init` — **do erro real do box do Werner** |
| #115 | os 8 `bin/check_*.py` não morrem em stdout cp1252 do Windows |
| #166 | ramo JSON do `read_table` não destrói escala decimal — **atinge API com dinheiro** |
| #147 | nomes de campo com espaço + payload posicional mais largo que o header |
| #114 | CA corporativa **une** o trust store em vez de estreitá-lo |
| #160 | `LOGGER` antes de qualquer resolução falível |
| #171 | gate de fronteira de vendor por camada + seam `utils.frames` |
| #172 | exemplo ORM não usa mais reader banido |
| #138 | *(fechada 16/08)* política deny-by-default por camada, com motivo — via #171 |

---

## A — Toca o caminho do duskko no dia 1

**Ingestão de API (o coração do projeto)**

- [x] **#128** — disciplina de contrato de ingestão (**8 lições**) + gates de colisão de nome e
      população de `__all__`. A mais densa da fila: `tuple_required` é um *interruptor de falha*,
      não descrição do arquivo; pinar o contrato **não** protege o reader; `series.any()` num
      frame vazio reprova membro legitimamente só-cabeçalho.
- [x] **#120** — seam `raw_workspace` (retenção de artefato bronze). A lição foi backportada como
      *orientação* e **o seam nunca foi entregue**. Existe em 2 dos 4 proving grounds. Toda
      ingestão baixa um artefato bruto — sem isso, ele é descartado.
- [x] **#150** — cachear download de vendor diário-estável dentro do seam. API da Tesouraria
      consultada várias vezes no mesmo dia = mesma resposta.
- [ ] **#116** — quebrar `utils/retry.py` em pacote `retry/` (4/4 proving grounds pedem). O seam
      de retry é o que toda leitura de rede usa.
- [ ] **#144** — o job de drift reporta a própria cegueira como "sem drift". Fonte inalcançável
      devolve `[]` e imprime tudo-certo. Um contrato de API muda sem avisar.
- [ ] **#148** — adaptadores por regime para mudança de schema no meio da série.
- [ ] **#117** — seam de XML (`read_xml`). **Baixa para o duskko** (API é JSON), mas está pronta.

**Persistência / queries — o duskko *substitui* SQL por API, mas grava em SQLite**

- [x] **#119** — layout `queries/<engine>/` + resolver `load_query` + guard de runtime para config
      git-ignorada. ⚠️ O diretório `src/config/queries/` **existe e está vazio** nos templates.
      O `contexto_duskko/config/consultas_sql/` tem exatamente essa forma.

**Ambiente Windows / corporativo (o box do Werner)**

- [x] **#125** — pinar `poetry-plugin-export` no instalador de bootstrap, não só como dev-dep.
      Uma receita funciona do venv ativado e falha de qualquer outro shell — que é o caso do
      Werner. Mesma família da #165 já entregue.
- [x] **#122** — `to_absolute` público em toda passagem para processo externo. Resolução de
      caminho é onde Windows quebra.
- [ ] **#127** — wheelhouse offline. **Rebaixada**: a #165 resolveu o quadro observado (índice
      *parcialmente* bloqueado). A #127 cobre o bloqueio *total*, que não é o que o Nexus faz.

**Segundo desenvolvedor / projeto bilíngue**

- [x] **#141** — gate de idioma da documentação. **A mais relevante desta seção.** A lição veio do
      dono corrigir comentários PT na `src/` **duas vezes na mesma sessão** — a segunda porque o
      sweep varreu só `.py` e deixou 26 em `emails.yaml`, que viraram o padrão copiado. Um sweep
      parcial deixa **precedente**, não dívida. ⚠️ O difícil está medido: primeiro rascunho deu
      **19 achados, 18 falsos** — case por palavras funcionais, nunca por acento.
- [x] **#126** — codespell também linta a mensagem de commit, e o `skip` não cobre isso.
      **Me mordeu 3× hoje** neste repo; num projeto bilíngue o Werner bate nisso na primeira semana.
- [ ] **#124** — `tests/CLAUDE.md` nas 4 tiers que não têm. É o que um dev novo lê para saber como
      testar aqui.
- [ ] **#152** — `tests/CLAUDE.md`: disciplina de controle negativo e prova (4 lições). Esta
      sessão inteira dependeu de controle negativo; sem isso escrito, não se propaga.

**CI do projeto gerado**

- [x] **#123** — o gate de work-ledger **bloqueia permanentemente todo PR de bot**. Dependabot no
      duskko fica travado, e o efeito real é treinar gente a usar `--admin`.
- [ ] **#111** — o cache de venv desliga o gate de instalação; falta teste should-fail em cada
      `check_*.py`. *(Metade entregue no #170: o gate de lockfile saiu de dentro do guard de cache.)*
- [x] **#146** — guard de pre-push: índice não-vazio no push significa commit rejeitado.
      **Aconteceu comigo hoje**, duas vezes.
- [ ] **#145** — endurecer o `pr_gate` (recusa GraphQL descartada, handover de merge, população de
      check obrigatório, gate de thread).
- [x] **#156** — gate de `actionlint`: o `yamllint` **não** valida workflow. Senti falta hoje.
- [ ] **#130** — `docs/architecture.md` é entregue e não tem gate.
- [ ] **#113** — nome de projeto com hífen gera pacote não-importável. **`duskko` não tem hífen**,
      então não bloqueia — mas é uma armadilha de 1 linha para o próximo projeto.
- [x] **#110** — bump `wwdates >=1.0.0` nas tiers de serviço. Trivial.  ✅ **PR #224 / v0.15.11**

**Entregável / e-mail (se o duskko reportar)**

- [ ] **#118** — `utils/ms_office` + `excel_sheet_names`. **Vira port**: 208 linhas + 18 testes
      prontos em `recon_al_cvm`. Nome de aba derivado de dado estoura 31 chars e o `openpyxl`
      trunca **em silêncio**.
- [ ] **#121** — metade de orquestração de e-mail (dispatch/sender/html_body).
- [ ] **#151** — degradação graciosa precisa de caminho a partir de **toda** falha (mvc-*).
- [ ] **#161** — filtro que remove registros de entregável precisa de kill switch e preço medido.
- [ ] **#162** — uma lista de problemas não carrega duas severidades.

---

## B — Qualidade da BlueprintX; não tocam o duskko no dia 1

- [x] **#173** *(`hitl`)* — PR gate no próprio repo. ~~Medido: nada bloqueia merge hoje~~
      **superado 2026-08-22:** `required_conversation_resolution=true` + 15 required
      status checks em `main` (medido na API). O merge recusado foi provado na PR #219 com
      controle negativo (o `github-advanced-security` reprova e a PR fica `CLEAN`, logo nao e
      ele). Resta o GitGuardian (#153/#155) — detalhe em
      `pr-gate-blocks-merge_20260817_104500.md`.
- [ ] **#169** — decisão sobre `ERA001`: 9 comentários reprovados, **zero** verdadeiros positivos,
      4 rodadas nesta sessão.
- [ ] **#164** — rodar `enable_repo_rules`/`enable_security` contra o próprio BlueprintX.
      Pré-requisito natural da #173.
- [x] **#139** — layer map do gate de fronteira em **2 de 5** tiers Python (falta `ddd-*` ×2 e  ✅ **PR #224 / v0.15.11**
      `lib-minimal`). ⚠️ **SUPERADO em 2026-08-23 (PR #224):** o gate NÃO se cala mais —
      árvore com módulos e sem policy agora FALHA, e as 4 tiers que faltavam (incluindo o
      próprio `python-common`) ganharam a sua. O texto original descrevia o estado anterior.
- [x] **#140** — **direção** entre camadas não é imposta: `model/` pode importar `controller/` e  ✅ **PR #224 / v0.15.11**
      passa. As exceções de vendor já foram entregues; resta só a direção.
- [x] **#174** — gate de thread de revisão (**já rodando**; resta roster nas demais tiers e
      `isOutdated` explícito).
- [ ] **#159** — gate de referências docs↔código.
- [x] **#163** — três regras de fronteira (seam/vendor, externalizar copy, derivar da config).  ✅ **PR #224 / v0.15.11**
- [x] **#167** — complexidade ciclomática (Python), com medição pronta.  ✅ **PR #224 / v0.15.11**
- [ ] **#149** — piso de cobertura derivado do código + auditar backlog semeado por proxy.
- [ ] **#153** / **#155** / **#129** — GitGuardian e topologia de PR-gate nos scaffolds.
- [ ] **#142** — workflow git offline. Só importa se o duskko **não** tiver remoto GitHub.
- [ ] **#109** — `doctor` para drift de template em projetos já scaffoldados. **Meta-issue:** é
      justamente o que torna barato scaffoldar antes do resto desta lista.

---

## C — Adiadas por decisão do dono

`ts-*` apenas: **#132, #133, #134, #135, #136** (ts-lib), **#168** (complexidade em ts),
**#175** (gate de thread em ts).

---

## Recomendação

**Nada aqui bloqueia o `make new`.** A base está publicada e verificada. Confirmado de novo em
2026-08-23: dos 16 itens restantes da seção A, **nenhum muda a FORMA do projeto gerado** — todos
são arquivo aditivo (um gate em `bin/`, um `tests/CLAUDE.md`, um subpacote em `utils/`), logo o
backfill é cópia, não retrabalho estrutural. O único da família "muda o layout" era a #119
(`queries/<engine>/`), e ela já foi entregue.

### Onda de preparação — 2026-08-23 (branch `feat/duskko-prep-wave-113`)

Corte executado antes de scaffoldar, escolhido por *custo de backfill*, não por ordem na lista:

- [ ] **#113** — hífen no nome do projeto gera pacote não-importável. É a única com custo
      one-shot real: `duskko` não tem hífen, mas o defeito está no `bin/scaffold/`, e o próximo
      projeto que tiver paga.
- [ ] **#124 + #152** — `tests/CLAUDE.md` nas 4 tiers sem ele (`python-common`, `ddd-*` ×2,
      `lib-minimal`), já com a seção de controle negativo. É o doc que o Werner lê.
- [ ] **#144** — job de drift que reporta a própria cegueira como "sem drift". O duskko é
      ingestão de API: contrato mudando calado é o risco central.
- [ ] **#118** — `utils/ms_office` + `excel_sheet_names` (lift de `recon_al_cvm`).
- [ ] **#121** — metade de orquestração de e-mail (lift de `recon_al_cvm`).

**Fora da onda, com motivo medido:**

| # | Por que não |
|---|---|
| #116, #117 | O lift viria de `filings-b3` / `filings-cvm` / `wwdates`, que **não existem nesta máquina** (`~/dev/` só tem `recon_al_cvm`). Não é cópia de 14/16 KB — é autoria do zero, e um seam de retry/XML autorado sem o original diverge do que 4 projetos já convergiram. |
| #109, #228 | Ferramentas novas (doctor de drift; seam de browser-steps), não backport. PR próprio. |
| #151 | Redesenho do orquestrador + matriz de 5 modos de falha. Não cabe junto. |

Comando depois do merge: `make new` → python → `mvc-service-native-db` → nome `duskko`.
