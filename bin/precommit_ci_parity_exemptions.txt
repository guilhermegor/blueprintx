# Data file read by bin/check_precommit_ci_parity.sh — one entry per line, pipe-delimited:
#   <script-basename>|<side>|<reason>
# <side> is "precommit_only" (a pre-commit hook runs it, no CI workflow does) or "ci_only"
# (a CI workflow runs it, no pre-commit hook does). A line with an empty reason is rejected
# by the script — the reason is what makes an exemption load-bearing rather than decorative
# (same shape as .review-bots.yaml / .layer-policy.yaml, blueprintx#384).
#
# Lines starting with "#" are comments. Blank lines are ignored.

smoke_test.sh|ci_only|blueprintx#384: dry-run-smoke scaffolds every one of the 6 skeletons on every push; pre-commit only reruns hooks whose files: pattern matches the staged diff, so a hook could never give the same matrix coverage (see the comment above dry-run-smoke in scaffold_checks.yml).
scaffold_lint_test.sh|ci_only|blueprintx#384: scaffold-lint-test / scaffold-lint-test-multi do a real scaffold + make lint + make unit_tests per tier — same reason as smoke_test.sh, too heavy for a file-pattern-triggered pre-commit hook.
check_gate_staleness.sh|ci_only|blueprintx#368: compares this PR's checkout against origin/<base> via the GitHub Actions PR/API context (github.event.pull_request, gh pr list) — a local pre-commit run has no PR or base-branch relationship to measure.
check_review_bot_roster_optout.sh|precommit_only|blueprintx#384: real gap, not yet wired into CI — tracked in blueprintx#510. Deferred rather than fixed here because scaffold_checks.yml was being edited by several other open PRs and this PR's own edit to it is scoped to exactly one job (the parity checker's own).
check_comment_budget.py|precommit_only|blueprintx#384: real gap, not yet wired into CI — tracked in blueprintx#510, same reason as check_review_bot_roster_optout.sh above.
