# Pre-commit / CI parity measurement (#384)

Verify "pre-commit and CI run the same checks" as a **measured** property, both sides
(BlueprintX root, and the pair shipped into every generated project). Join key is the
**script actually invoked** (a hook's `entry:` / a CI step's `run:`), never job or hook
names — a name-join is nonsense on the scaffold side (51 hooks vs 1 job). All 12 root
workflow files were scanned, not only `scaffold_checks.yml` — a single-file read reports a
false gap (`check_secrets.sh` looked pre-commit-only until `secret_scan.yml` was read).

- [x] Enumerate root `.pre-commit-config.yaml` hook scripts vs all 12
      `.github/workflows/*.yml` script invocations.
- [x] Enumerate `templates/python-common/.pre-commit-config.yaml` vs
      `templates/python-common/.github/workflows/tests.yaml` (read-only — that tree is
      reserved for PR #403 / issue #407 / the in-flight rmw-gate work, not edited here).
- [x] Classify every divergence: deliberate+reason / deliberate+no-reason / accidental.
- [x] Fix what's fixable inside this agent's surface (root `.pre-commit-config.yaml`,
      `.github/workflows/scaffold_checks.yml`, `docs/`).
- [ ] Wire the assertion into `check_gate_integrity.py` (templates/python-common — out of
      surface; tracked for whoever picks up #384's remaining scope alongside #359).

## Root: 19 gate scripts compared, 3 divergent, 0 accidental

| Script | Side | Class |
|---|---|---|
| 16 scripts (check_spelling, check_codespell_sync, check_shell, check_git_remote_guard, check_actions, check_function_length, check_complexity, check_gate_integrity, check_secrets, check_test_copy_lists, check_project_name_identities, validate_meta, check_version_sync, check_docs_build, check_makefile_pairing, check_docs_code_refs) | both | parity — no action |
| `bin/ci/scaffold_lint_test.sh` | CI only | **deliberate, reason written** (CLAUDE.md: real verification needs a generated project + installed deps; the template root is a false green) |
| `templates/common/bin/check_review_threads.py` | CI only | **deliberate, reason written** (CLAUDE.md: takes its subject from `GITHUB_REPOSITORY`/`PR_NUMBER`, which do not exist locally) |
| `bin/ci/smoke_test.sh` | CI only | **deliberate, reason was NOT written — fixed here.** Added a comment in `scaffold_checks.yml` (`dry-run-smoke` job): pre-commit only re-runs a hook whose `files:` pattern matches the staged diff, so a `skeleton.meta` change would trigger one skeleton, never the full six-skeleton sweep this matrix gives on every push. |

`check_secrets.sh` (gitleaks) is on **both** sides — pre-commit hook `check-secrets`, and CI via
`secret_scan.yml` (not `scaffold_checks.yml`). Confirms the single-file false positive the
issue named. `scaffold_checks.yml` also gained a separate `secret-scan` (GitGuardian/ggshield)
job since the issue was filed — a second tool, not a script-name match, out of scope for this
join and not wired into required checks yet (documented in-file as unmeasured).

## Scaffold (shipped to every generated project): 4 divergent — none fixable from this surface

Read-only measurement; `templates/python-common/**` is reserved (PR #403 owns
`.pre-commit-config.yaml`, issue #407 owns `validate_meta.sh`/`tests/`, and a separate
in-flight gate — `check_rmw_race.py` — is mid-add). No edits made here.

| Script | Side | Class |
|---|---|---|
| `bin/check_clean_index.sh` | pre-commit only | **deliberate, reason written** (`CLAUDE.md`: a pre-push hook guarding the git index — CI only sees what was already pushed, so there is no CI-side equivalent to have) |
| `bin/test_urls_docstrings.sh` | pre-commit only | **deliberate, reason NOT written** — network check with a 1-week cache; the common reason to keep a flaky network check out of CI (avoid failing PRs on transient DNS/host issues) is never stated in `templates/python-common/CLAUDE.md` or `bin/CLAUDE.md`. Needs a written reason in `templates/python-common/CLAUDE.md`, which is outside this agent's surface. |
| `bin/lint_docker.sh` | CI only (`tests.yaml`) | **accidental.** Root `CLAUDE.md` states "Pre-commit hooks … ruff, pydocstyle …, hadolint, …" but `templates/python-common/.pre-commit-config.yaml` has no hadolint/`lint_docker.sh` hook today — the doc and the file disagree. Either add the hook or correct the doc; both live in reserved territory (PR #403 / template CLAUDE.md). |
| `bin/check_rmw_race.py` | pre-commit only (new since this issue was filed) | **unclassified — in flight.** Present in `templates/python-common/.pre-commit-config.yaml` with no `tests.yaml` mirror yet; matches the active `rmw-gate-385` work, not this issue's scope. Flagging so it isn't lost, not fixing it here. |

Excluded from the join entirely (automation/bots, never gates, so their CI-only presence is not
a divergence): `bin/enable_repo_rules.sh`, `bin/rerun_stale_gate_runs.sh`,
`bin/retry_rate_limited_review.py`, `bin/blueprintx.sh` (packaged into release artifacts, not
invoked as a check).

## Totals

- **Deliberate, reason already written:** 3 (`scaffold_lint_test.sh`, `check_review_threads.py`,
  `check_clean_index.sh`)
- **Deliberate, reason missing → written this round:** 1 (`smoke_test.sh`, root side, fixed)
- **Deliberate, reason missing → still undocumented (out of surface):** 1
  (`test_urls_docstrings.sh`)
- **Accidental:** 1 (`lint_docker.sh` vs the CLAUDE.md hadolint claim — out of surface)
- **In flight / unclassified:** 1 (`check_rmw_race.py`)

Completed for this agent's surface — kept as a record. The out-of-surface rows (2 accidental/
undocumented, 1 in-flight) are handoffs, not closed items; whoever next touches
`templates/python-common/.pre-commit-config.yaml` or its `CLAUDE.md` should resolve them.
