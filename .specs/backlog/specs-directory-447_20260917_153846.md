# .specs/ directory organization (blueprintx#447)

## Done

- [x] `.specs/CLAUDE.md` — the `.specs/` convention (`features/<name>/{design.md,plan.md}`,
      git-ignored `_lessons/` mirror), matching the global user-CLAUDE.md redirect rule
      ("project has a `.specs/` at its root already → write there").
- [x] Migrated all 10 pre-existing `docs/superpowers/{specs,plans}/*.md` files into
      `.specs/features/<name>/{design.md,plan.md}` (`git mv`, history preserved).
      `docs/superpowers/` is now empty on disk.
- [x] `bin/ci/check_specs_structure.sh` — structure guard (CLAUDE.md present, only
      `CLAUDE.md`/`features/`/`_lessons/` at the top level, every `features/<name>/` has a
      `design.md` or `plan.md`). Wired into `.pre-commit-config.yaml` (`specs-structure`
      hook) and `.github/workflows/scaffold_checks.yml` (`specs-structure` job) — one
      implementation, same pattern as every other gate in this repo.

## Deliberately deferred (not in this PR)

- [ ] Drop the now-stale `superpowers/` line from `mkdocs.yml`'s `exclude_docs:` and remove
      the `superpowers/` row from `docs/CLAUDE.md`'s "Subdirectory pattern" table + example
      snippet. Both files are currently held by several other open PRs (#476/#503/#508 for
      `mkdocs.yml`, #508 for `docs/CLAUDE.md`) — skipped to avoid collision. Harmless in the
      meantime: `exclude_docs` glob for an empty/absent directory is a no-op, and
      `mkdocs build --strict` already passes with `docs/superpowers/` empty.
- [ ] `docs/superpowers/{specs,plans}/` empty directories themselves are not git-tracked
      (git doesn't track empty dirs), so nothing further to clean up there.

## Notes

- "Redirect writers before moving files" (the issue's phrasing) is satisfied by the global
  `~/.claude/CLAUDE.md` rule already in place on this machine — once `.specs/CLAUDE.md`
  exists, `s:brainstorming`/`s:writing-plans` land here automatically. Nothing to change
  in-repo for that half.
- `_lessons/` is intentionally not created empty in this PR — it's populated by local
  tooling and PR #501 (in flight) adds it to `.gitignore`; the structure guard tolerates
  its absence.

Completed — kept as a record. The deferred items above are a real, tracked follow-up, not
lost work: pick them up once #476/#503/#508 have merged and `mkdocs.yml`/`docs/CLAUDE.md`
are free again.
