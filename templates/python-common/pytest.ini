[pytest]
asyncio_mode = auto
pythonpath = . src
addopts = --strict-markers
filterwarnings =
    error:Missing docstring for parameter.*:

    # A ResourceWarning raised inside __del__ is UNRAISABLE — Python cannot propagate it out of
    # a finalizer, so pytest catches it and re-emits it as PytestUnraisableExceptionWarning
    # instead. `error::ResourceWarning` alone never sees a warning of that re-emitted class, so
    # it never escalates: a file handle leaked by a garbage-collected object passes silently
    # (measured, blueprintx#294 — the exact leak shape #223 shipped). The second line is not
    # redundant with the first: it is the entire mechanism for the __del__ case, which is the
    # most common way a leak actually happens. Do not delete it as noise.
    error::ResourceWarning
    error::pytest.PytestUnraisableExceptionWarning

    ignore:divide by zero encountered in log:RuntimeWarning
    ignore:divide by zero encountered in scalar divide:RuntimeWarning
    ignore:divide by zero encountered.*:RuntimeWarning

    ignore:The total space of parameters.*is smaller than n_iter.*:UserWarning

    # ⚠️ Ignore a warning by SOURCE, never by class alone. A bare `ignore::DeprecationWarning`
    # hides every deprecation in the process, including the ones aimed at THIS code — and a
    # template that hides them ships that blindness into every generated project, exactly when
    # a pandas or SQLAlchemy deprecation is the thing you most want to read. Measured before
    # narrowing these: with the four blanket lines removed the suite emitted 4 warnings, all
    # from one vendor. They were hiding almost nothing and costing the signal entirely.
    ignore::DeprecationWarning:PIL
    ignore::DeprecationWarning:PIL.*
    ignore:'mode' parameter is deprecated.*:DeprecationWarning
    ignore:'mode' parameter is deprecated.*:DeprecationWarning:PIL._deprecate
    ignore:Deprecated attribute .*:DeprecationWarning:beartype.*

    ignore::UserWarning:matplotlib

    ignore:indexing past lexsort depth may impact performance
markers =
    timeout: mark test to have timeout (durations in seconds)
    performance: mark test as a performance test
    allow_network: opt a test out of the autouse network block (tests/conftest.py)
