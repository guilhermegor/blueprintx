# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this directory is

`templates/python-common/` is the **single source of truth for shared tooling** across all BlueprintX Python skeletons. Every file here is copied verbatim (or rendered via `envsubst`) into scaffolded projects by each `bin/scaffold/python_*.sh` script.

**Changes here propagate to all Python skeletons on the next scaffold run.**

Most of this directory is *tooling* (ruff, pytest, poe_tasks.toml, bin scripts). Two subtrees are the **deliberate exceptions** — they hold *application code* that is identical across skeletons, so it lives here rather than being duplicated:

- **`src/config/`** — the global runtime config (`startup.py`, `inputs.yaml`, `outputs.yaml`). **Always** copied into every service project's `src/config/`. This is why the four service skeletons no longer carry their own `startup.py`/`inputs.yaml`/`outputs.yaml`.
- **`src/utils/`** — project-agnostic helpers (`br_identifiers.py`, `dtypes.py`, `decimals.py`, `logs.py`, `text.py`, `paths.py`, `signatures.py`, `dates.py`) plus their unit tests. **Always** copied into every service project's `src/utils/` and `tests/unit/`, so the helpers exist in every Python skeleton from one source. Every helper carries the runtime checker (`@type_checker` on functions, `metaclass=TypeChecker` on classes) — there is no by-layer exemption. Because the files ship to both tiers, they import the engine through a layout-agnostic shim (`try: from utils.typing import … except ModuleNotFoundError: from chassis.typing import …`), so the same source resolves under `utils.typing` (MVC) and `chassis.typing` (DDD).
- **`optional/`** — opt-in app code injected when the matching scaffold prompt is answered *yes* (or, for `typing`, always). Shipped into the generated project's normal layout, so the output looks standard — the `optional/` marker exists only here, to keep the tooling-vs-app-code boundary visible. Contains `webhook/` (opt-in port-based notifier), `chassis/` (opt-in storage), and `typing/` (the runtime type-checking engine — copied to `src/chassis/typing/` for DDD, `src/utils/typing/` for MVC via a `chassis.typing → utils.typing` rewrite, mirroring the webhook seam).

## Files and their roles

| File / Path | Role |
|-------------|------|
| `ruff.toml` | Ruff lint + format config (line-length 99, tab indent, double quotes, NumPy docstrings, full rule set) |
| `.pre-commit-config.yaml` | Hooks: ruff, pydocstyle (DAR), codespell, commitizen, gitlint, hadolint, unit + integration tests, coverage badge |
| `pytest.ini` | Pytest configuration shared by all generated projects. A standalone per-tool file (like `ruff.toml` / `mypy.ini`), **not** a `[tool.pytest.ini_options]` block. `pythonpath = . src` — both, so the app's bare imports (`config.x`, `utils.x`) resolve exactly as they do at runtime. ⚠️ **Never `-p no:warnings`**: that disables the warnings plugin outright, hiding everything including whatever appears next. This file does the opposite — it decides per warning, and even promotes one to a FAILURE (`error:Missing docstring for parameter.*`). By the same rule an ignore names its SOURCE, never a bare class: a blanket `ignore::DeprecationWarning` hides the deprecations aimed at this code, and a template that hides them ships that blindness into every generated project — precisely when a pandas or SQLAlchemy deprecation is what you most want to read. Measured when the four blanket lines were removed: 4 warnings, all from one vendor, so they were hiding almost nothing and costing the signal entirely |
| `poe_tasks.toml` | **The command interface** (Poe the Poet), replacing the old `Makefile` + `tasks.sh` pair — two hand-maintained implementations of one command list, kept in sync by a written rule that had already been broken (blueprintx#189). Tasks: `init` (`ensure_env` → `venv` → `precommit` → the three GitHub enablers), testing, linting, database, `run`, `export_*`, `ship`, docs. ⚠️ **Tasks run inside the project venv automatically** — poe detects the in-project `.venv` (forced by `poetry.toml`), so a task is a BARE command (`pytest tests/unit/`), never `poetry run pytest`. `bin/poetry_exec.sh` survives only for Poetry MANAGEMENT commands (`update`, `install --with docs`, `version`), which act on the venv instead of running inside it. ⚠️ **No help file**: bare `poe` prints the list from each task's `help =` field, so help and recipe cannot drift. ⚠️ **No `test_feat` task** — poe passes extra CLI tokens through to any task with no declared `args`, so `poe unit_tests -k kw` is the filter for free. Conditional sets (`poe_tasks.offline.toml`, `poe_tasks.library.toml`) are wired by the scaffold via `add_poe_include`, never declared unconditionally: unlike make's silent `-include`, a missing poe include WARNS on every invocation |
`pyproject.toml` in blueprintx#233): OFFLINE keeps `bump_version` (`cz bump`); ONLINE the scaffold **strips** `bump_version` (`strip_bump_version` in `bin/lib/common.sh`) — releases are cut by the `release.yaml` workflow (services: tag + GitHub Release, no PyPI) or `release-pypi.yaml` (lib). The docs build regenerates `CHANGELOG.md` via `cz changelog`, so the published Changelog page tracks the tags |
| `.gitlint` | gitlint length limits (title ≤ 72, body line ≤ 80) — made explicit so they are not discovered only on a late hook rejection |
| `.vscode/settings.json` | Shipped VS Code settings: POSIX interpreter default (Windows path commented), `src` analysis path, pytest, tab-4 Python, INI `files.associations` for `.gitlint`/`.codespellrc`/`.pydocstyle`, poetry env manager. **Static fast-feedback loop:** `python.analysis.typeCheckingMode: strict` (Pylance/pyright flags wrong-typed annotations as-you-type) + Ruff format/autofix/organise-imports on save, scoped to `[python]`. No config is duplicated — `ruff.toml` + `mypy.ini` stay authoritative. Complements the runtime checker (which fires only when code runs) |
| `.vscode/extensions.json` | Recommended extensions offered on first open — `charliermarsh.ruff`, `ms-python.python`, `ms-python.vscode-pylance` — backing the settings above |
| `export_context` (target) | Runs `bin/export_repo_content.sh` — script is sourced from `templates/common/bin/` (language-agnostic), copied into the project `bin/` by each scaffold |
| `requirements.txt` | Pins the **bootstrap installer's** set — Poetry plus its plugins (`poetry-plugin-export`, `poetry-plugin-shell`); never application dependencies. A Poetry plugin belongs here and **not** as a dev-dep: `export` and `shell` are plugin subcommands, and a dev-dep reaches the project venv (where no Poetry lives to load it) and nothing else. The two installs report the **same `--version`** while carrying different plugin sets, so every version probe agrees and the subcommand still does not exist. ⚠️ **Four of the five Python scaffolds never copied this file** — `poetry export` shipped without its plugin in every service tier while `lib-minimal` alone had it. Fixed; it is exactly the hand-maintained-cp-list drift that keeps recurring, so a new root-level shared file must be added to **all five** scaffolds |
| `.python-version` | pyenv Python version pin |
| `.gitignore` | Python + Poetry + common IDE patterns |
| `.codespellrc` | codespell configuration. The **single source both gates read**: `poe lint` scans the whole working tree while pre-commit scans only staged files, so they agree only because `skip`/`ignore-words-list` live here. Two distinct classes of `skip`: generated artifacts (`htmlcov`, `coverage.svg`, `*.diff`) and **`*-lessons.md`** — the git-ignored mirror the lessons-capture workflow *writes*; without it, writing a lesson flags a file outside your diff and aborts an unrelated staged commit. ⚠️ Use the **basename** glob, never `docs/*-lessons.md`: `poe lint`/CI run `codespell .`, whose paths come back as `./docs/…`, which a `docs/`-anchored pattern silently fails to match |
| `.pydocstyle` | pydocstyle config (NumPy convention, DAR checks) |
| `.coveragerc` | Coverage.py configuration (omits chassis, example_feature, app, config) |
| `mypy.ini` | Standalone mypy config, invoked from `src/` as the single package base. Excludes **only** the runtime type-checking engine (intrinsic metaprogramming). ⚠️ `exclude` filters file DISCOVERY and does NOT apply to a module reached through an `import`, so anything that must stay unchecked needs a `[mypy-<tree>.*] ignore_errors` section, not a path — and `ignore_errors` is a holding position, not a destination. `^chassis/` and `^capabilities/example_feature/` were both removed in blueprintx#190: the first was genuinely unchecked and had accumulated 20 errors, the second was excluded and checked anyway because `main.py` imports it. Ruff still excludes both — deliberate divergence, tracked in blueprintx#203 |
| `poetry.toml` | Poetry local config — forces `virtualenvs.in-project = true`. ⚠️ **Not** a second home for `[tool.poetry]` — see "What can leave `pyproject.toml`" below |
| `.cz.toml` | Commitizen config, moved out of `pyproject.toml` (blueprintx#233). ONE file copied to all five tiers — the keys were byte-identical, so five copies would have been the drift `check_codespell_sync.sh` exists to police. ⚠️ **Its loss is silent**: cz tries `pyproject.toml` first, skips it when there is no `[tool.commitizen]`, falls through to here — and when this file is absent too it does **not** error. Measured: `cz version --project` prints `No project information in this project.` and exits **0**, so the symptom is an unprefixed tag and a differently formatted changelog months later, never a red check. That is why `bin/ci/scaffold_lint_test.sh` asserts cz can still answer in a generated project, rather than asserting the file was copied |
| `README.md` | Template README with `${VARIABLE}` placeholders for `envsubst` |
| `.github/workflows/tests.yaml` | CI workflow — copied into the project **only** when a GitHub remote is set up (see `copy_github_assets` in the scaffolds) |
| _(CODEOWNERS, PR template)_ | Language-agnostic; live in `templates/common/.github/`; also GitHub-remote-only |
| `docker-compose.{postgresql,mariadb,mysql}.yml` | DB infra templates; one is copied to `docker-compose.yml` on the docker-compose prompt |
| `src/config/startup.py` | Global runtime config — logger + `output_path()` helper + `PATH_LOG/JSON/TXT`. Always copied into every service project. |
| `src/config/inputs.yaml` | Global inputs — single output root (`daily_infos_base_path`, default `logs`) + `daily_infos_dated` toggle |
| `src/config/outputs.yaml` | Global filename templates (`log/json/txt/csv/xlsx_name`) using **named** placeholders |
| `src/utils/br_identifiers.py` | CNPJ/CPF `mask_*` / `unmask_*` / `is_valid_*` — alphanumeric-aware (2026 CNPJ), pure functions. Copied into every service project's `src/utils/` |
| `src/utils/dtypes.py` | `apply_dtypes(df, dict_dtypes, list_date_cols, list_datetime_cols)` — enforces explicit column typing on load. Copied into every service project's `src/utils/` |
| `src/utils/provenance.py` | `hash_artifact(path)` + `stamp_provenance(df, url, contract, content_hash, package_version)` — appends the six provenance columns (`url`/`updated_at`/`source_key`/`package_version`/`ingestion_run_id`/`content_hash`) to an internet-ingested frame after its source columns; `updated_at` is tz-aware UTC. The stamp is a **pure** transform; the hash is I/O. Column names live on `FileContract.PROVENANCE_COLUMNS`; paired with `read_table` by `bin/check_provenance.py`. See `src/config/CLAUDE.md` |
| `src/utils/sidecar_metadata.py` | Generic sidecar schema-descriptor seam: `cvm_meta_url(base, key)` (reference locator → `<base>/META/meta_<key>.txt`), `fetch_sidecar_text(url, path_raw, fn_download=…)` (persists the descriptor to bronze and returns its text, or **`None`** when the source has none — tolerant, never raises), `parse_sidecar_metadata(text)` → `{field: {column: value}}`. Locator + transport injectable; CVM is the documented reference, not hard-coded. See `src/config/CLAUDE.md` |
| `src/utils/retry/` | Retry-with-backoff seam for transient I/O — a **package**, not a module (blueprintx#116). `policy.py` (`RetryPolicy`, frozen value object), `_schedule.py` (private wait math), `log_emitter.py` (injectable sink), `backoff.py` (`call_with_backoff` imperative + `retry_with_backoff` decorator), `__init__.py` re-exporting the four public names. The split is what **all four proving grounds converged on independently** — the strongest backport signal in the corpus, stronger than any single written lesson. Three schedules (`exponential`/`linear`/`constant`) via **dict dispatch**, with `_STRATEGIES = frozenset(_STRATEGY_WAITS)` so the valid-name set cannot drift from the branch; optional `float_max_wait_s` caps an otherwise unbounded exponential. ⚠️ Deterministic failures are **not** retried (only `tuple_exceptions`), so a bad URL still fails fast. `call_with_backoff` takes `str_label` because a `lambda` logs as `<lambda>`, naming the wrapper instead of the operation — and the log is all a scheduled run leaves behind |
| `src/utils/decimals.py` | `to_decimal(value, int_places, default, rounding)` — precise `Decimal` coercion (BR-number aware), **ROUND_DOWN default**, per-call override |
| `src/utils/logs.py` | `CreateLog` + `log_message` + `initiate_logging` — in-repo logging helper (no external dependency) |
| `src/utils/text.py` | `normalize_text` — NFKD + accent-strip + casefold; used for allow-list matching (e.g. the webhook production-env gate) |
| `src/utils/paths.py` | `is_windows_path` / `resolve_path` / `ensure_dir` / `to_absolute` — OS-independent path resolution (Windows paths parsed on POSIX). ⚠️ `to_absolute` is **public on purpose** and must be called at every hand-off to a **foreign process** (attachment argv, COM `SaveAsFile` destination, subprocess arguments) — it is an *output* rule, not an input one. Both failure modes are invisible to an `exists()` guard because that guard runs in **our** process: a CWD-relative path re-anchors in theirs, and a POSIX-shaped value on Windows is **drive-relative** (rooted but driveless → `is_absolute()` is `False`), so it lands on whichever drive the *reading* process sits on |
| `src/utils/signatures.py` | `resolve_signature` / `to_html` — e-mail signature HTML resolution |
| `src/utils/dates.py` | ANBIMA business-day helpers (`is_working_day`, `add_working_days`, `delta_working_days`, …) wrapping wwdates `DatesBRAnbima` |
| `src/utils/xml_reader.py` | `read_xml` (blueprintx#117) — the XML sibling of `tabular_reader.read_table`, sharing its `FileContract`/`ContractError`/dtype tail. One repeating row-anchor element yields one row; every other column is an ORDERED tuple of alternative paths resolved relative to that row, matched by **local name** (namespace-agnostic), with a single-level `*` wildcard and a trailing `@name` segment to read an **attribute** instead of element text — the #112 rule, since a money value's currency in ISO-20022-shaped documents lives in an attribute, not text (measured: 142,164 prices shipped with no currency under a text-only reader). A leading-`/` path is resolved once against the document root and **broadcast** to every row. `str_row_filter` keeps only row-anchor elements where a relative path resolves (presence, not value), applied before contract validation, so one heterogeneous file yields per-type frames without re-anchoring. `is_attribute_path` / `text_path_columns` exclude attribute-sourced columns from any layout/drift check, since an attribute is per-value metadata, not element layout. Parses via `defusedxml`, never bare `xml.etree` — the same trust-boundary rule `tabular_reader` applies to `pd.read_*` |
| `src/utils/ms_office/` | Vendor-scoped subpackage (blueprintx#118) — Microsoft Office helpers, one module per app. `outlook_gateway.py` (moved from `src/utils/outlook_gateway.py`) is unchanged Outlook COM automation; `excel_sheet_names.py` is new: `find_sheet_name_problems` / `find_workbook_sheet_name_problems` validate a worksheet name against Excel's own rules (≤31 chars, forbidden `[]:*?/\`, no leading/trailing whitespace, no leading/trailing apostrophe, the reserved name "History", no invisible/control Unicode characters named via `unicodedata.name`) plus case-**insensitive** cross-name uniqueness (`DADOS`/`Dados` collide). ⚠️ Deliberately **no** cell-reference-shape rule — `Q1`/`A1`-shaped names ARE valid worksheet names (verified against Microsoft's own docs); that restriction is real but applies to *defined names* (Name Manager), a different namespace. A runtime guard at the write boundary: `openpyxl`/Excel truncate or substitute an invalid name **silently**, with no exception to catch |
| `src/utils/email/` | Backend-agnostic e-mail capabilities (blueprintx#118/#121) — sibling of `ms_office/`: what any backend needs, not what one vendor does. `dispatch.py` (`resolve_dispatch`, moved out of `outlook_gateway.py`) resolves `EMAIL_SEND__<BLOCK>` / `EMAIL_AUTO_SEND__<BLOCK>` from `.env` (fallback `__DEFAULTS`, hard default send-on/auto-send-off) and **logs every consulted variable name and whether it was set** — a flag derived from a renamed config key that is read under a stale name would otherwise fall back to the default with nothing saying which key it looked for. `html_body.py` (`to_html_body`, also moved) turns bare newlines into `<br>` for an HTML-body client. `sender.py` (`send_email_block`) is the orchestration seam: resolves dispatch, then calls an **injected** sender callable (typically `EmailHandler.send_email` bound) only when the block may send — injected as a `Callable`, not the `EmailHandler` port type, because this package ships unconditionally while the port lives beside its adapters (`optional/email/`, whose location differs by layout) |
| `src/utils/CLAUDE.md` | Leaf doc for `utils/` (blueprintx#118) — the seam rule, the `find_*_problems` convention, the runtime-typing layout shim (copy-verbatim block), scalar/Series twins, and the vendor-subpackage placement test. Excluded from the distributed wheel like every other in-tree `CLAUDE.md` (`pyproject.toml`'s `exclude = ["src/**/CLAUDE.md"]`) |
| `optional/typing/` | Runtime type-checking engine (`TypeChecker`, `ABCTypeCheckerMeta`, `ProtocolTypeCheckerMeta`, `@type_checker`, `validate_type`). **Always injected** — to `src/chassis/typing/` (DDD) or `src/utils/typing/` (MVC, via `chassis.typing → utils.typing` rewrite). **Backed by `beartype`** (`validate.py` is a thin adapter, not a hand-rolled walker) — do not reimplement it. The tunable **policy** lives in `policy.py` (the editable seam), not in the adapter: `STRICT_BOOL` (reject `bool` as `int`), `WIDEN_INT_TO_NUMPY`, `PEP484_NUMERIC_TOWER` (opt-in), and `VIOLATION_TYPE` — ⚠️ **load-bearing: keep it `TypeError`** (beartype's own exception is not a `TypeError` subclass, and downstream `pytest.raises(TypeError)` depends on it). Flip a knob + the config rebuilds on import; never edit `validate.py` to change policy. Behavioural notes: mocks must be `spec=`-ed to pass; container checks are **sampled O(1)**, not exhaustive |
| `optional/chassis/db/` | `DatabaseHandler` ABC bundle — injected with the schema-less storage opt-in (and always for the native DDD skeleton, whose `db_schema` requires it). `infrastructure/helpers.py` also holds **`DsnParts`**, the TypedDict every SQL handler's `_parse_dsn` returns. ⚠️ Do not "simplify" it back to `dict[str, object]`: that erases each field on the way out, so `int(parts["port"])` has no overload, `env["PGPASSWORD"] = parts["password"]` is an incompatible assignment, and an argv built from those fields is a `list[object]` that `subprocess.run` rejects — one lossy return type was 14 of the 20 errors of blueprintx#190 |
| `optional/chassis/db_wschema/` | Schema-less storage (JSON/CSV/joblib) — injected on the storage opt-in (DDD only) |
| `optional/storage.env.fragment` | `.env` block appended on the storage opt-in |
| `optional/webhook/` | Port-based webhook provider (`WebhookNotifier` port + teams/slack adapters + `NullNotifier` + `build_webhook(url)`/`detect_platform(url)` factory) — injected on the webhook opt-in; lands in `chassis/webhook` (DDD) or `utils/webhook` (MVC). Platform is inferred from the URL; a blank URL yields a no-op `NullNotifier` |
| `optional/webhooks.yaml` | Webhook message config (named placeholders) — copied on the webhook opt-in |
| `bin/CLAUDE.md` | Shell-script conventions for every `*.sh` in `bin/` (incl. the never-bare-Poetry rule + "Testing shell scripts" convention) |
| `bin/poe_exec.sh` | Single **Poe** entrypoint for every hook, workflow and script — resolves `.venv/bin/poe` → `poe` on PATH → `$PYTHON -m poethepoet` → `poetry poe` (plugin) and `exec`s it. ⚠️ The module is `poethepoet`, **not** `poe`. The in-project-venv branch is load-bearing, not an optimisation: after `poetry install` the venv's poe is not on PATH and `$PYTHON` is the SYSTEM interpreter, so nothing else finds it — exactly the CI case |
| `bin/poetry_exec.sh` | Single Poetry entrypoint for every poe task and pre-commit hook that needs a Poetry MANAGEMENT command — resolves Poetry (`poetry` → `python -m poetry`) and `exec`s it, routing resolution status to stderr so `$(… version -s)` stays clean. No recipe calls a bare `poetry` |
| `bin/export_deps.sh` | `poe export_deps` — exports the locked set to `requirements-lock.txt` for hosts that can `pip install -r` but not run Poetry (corporate boxes, slim containers, air-gapped transfers). Routes through `ensure_poetry` + `run_poetry`, never a bare `poetry`. ⚠️ **Captures the export output and re-prints it on failure** — never `>/dev/null 2>&1` a command whose failure you then explain, or the explanation becomes a guess about text you refused to read; the remedy names `${POETRY_CMD[*]}`, the binary that actually failed |
| `bin/ensure_env.sh` | `poe ensure_env` / first `init` step — seeds `.env` from `.env.example` (no-op if `.env` exists; aborts only itself on a missing template, so a failed seed never blocks venv + precommit) |
| `bin/precommit.sh` | `poe precommit` — installs pre-commit hooks (commit-msg + pre-push), resolving Poetry via the bootstrap lib. **Skips gracefully** when run off a git work tree (a shipped zip with no `.git`) so `init` still completes |
| `bin/lib/common.sh` | Canonical sourced lib: `print_status`, `_read_env_var`, `resolve_default_branch` (main/master detection), color vars. **Kept byte-identical with `templates/common/bin/lib/common.sh`** — offline scaffolds copy the common-tier version over this one, so both must carry the same functions (do not let them drift) |
| `bin/lib/bootstrap.sh` | Sourced lib: cross-platform resolvers (`bootstrap_init`, `detect_os`, `resolve_python`, `ensure_poetry`), pyenv-preferred/system-Python `ensure_python_version`, `to_native_path` (cygpath path resolution), and `wire_corporate_ca`. Shared by `venv.sh`/`run.sh`/`get_corporate_ca.sh` |
| `bin/lib/pip_fallback.sh` | Sourced lib: Poetry→pip fallback for restricted hosts — parses `pyproject.toml` (Poetry + PEP 621) into pip requirements and installs groups straight into `.venv`. Sourced by `venv.sh`/`run.sh` |
| `bin/venv.sh` | Poetry venv setup — pyenv-preferred with system-Python fallback; configures in-project Poetry venv, upgrades pip in-env, installs `dev,docs`; **falls back to stdlib venv + `pip_fallback`** when Poetry is unavailable. Playwright browser install is **best-effort + OS-aware** (`try_install_playwright_browsers`: `--with-deps` on Linux only; a failure warns and never aborts `init`). Pyenv honoured on both branches |
| _`bin/new_branch.sh`_ | **Offline-only** — lives in `templates/common/bin/`, copied by `apply_offline_mode`, not from here. Create a feature branch off the default branch with CONTRIBUTING-prefix validation; `poe new_branch <x>` |
| _`bin/git_merge_to_main.sh`_ | **Offline-only** — lives in `templates/common/bin/`, copied by `apply_offline_mode`. Merge the current clean branch into the default branch and delete it (uses `--no-verify` to bypass `protect-branch`); `poe git_merge_to_main` |
| `bin/get_corporate_ca.sh` | Manual generator for `bin/corporate_ca.pem` — extracts a TLS-inspecting proxy's CA for pypi.org. The pem is git-ignored; its presence opts a project into corporate-SSL mode on the next `poe venv` |
| `bin/enable_repo_rules.sh` | `poe enable_repo_rules` (in `init`): provisions the **`pr-quality-gate`** ruleset on `~DEFAULT_BRANCH` (idempotent — looked up **by name**, PUT if present / POST if not, never a blind POST) plus the settings the PR gate cannot set itself: CodeQL default setup, `allow_auto_merge` (**read back** — without it native auto-merge silently no-ops), `delete_branch_on_merge`, and the `do-not-merge` label. ⚠️ `required_approving_review_count` **must stay 0** (GitHub forbids self-approval → any ≥1 locks a solo maintainer out); `REQUIRED_CHECKS` ships as exactly `("Review threads answered")` — one entry, and the choice of that one is the whole point. A required check that never reports blocks every PR forever, which is why the reviewer's OWN status (CodeRabbit, Copilot) is **not** listed: it may not report on a bot PR, or at all when the service errors. `Review threads answered` is ours, runs on `pull_request`, and always reports — and `find_missing_review_problem` makes it fail when NO reviewer ran at all, not just when a thread is open (blueprintx#213 merged unreviewed with every check green). ⚠️ **Without a ruleset the list does nothing**: the merge button stays enabled while a review is still in flight — which is BlueprintX's own state today, tracked in blueprintx#164. Admin-gated, non-blocking |
| `bin/enable_security.sh` | `poe enable_security` (in `init`): PUTs the three free security toggles — private vulnerability reporting, Dependabot alerts, then Dependabot **security** updates (**last**, it depends on alerts). Admin-gated, idempotent, non-blocking |
| `.github/dependabot.yml` | Ordinary **version** bumps (distinct from the security updates above, which are a repo toggle). `versioning-strategy: lockfile-only` — refreshes `poetry.lock` so CI tests ~what consumers install, but never rewrites the `pyproject` ranges (a library must not narrow ranges for consumers) and never trips the CI lock-sync guard. minor+patch grouped; majors arrive solo; `pandas` major ignored (a deliberate 2.x/3.x support decision). GitHub-remote-only |
| _(SECURITY.md)_ | Language-agnostic; lives in `templates/common/SECURITY.md` with `${PROJECT_DISPLAY_NAME}`/`${REPOSITORY}` placeholders. GitHub auto-detects it at the repo root (no API call). English — a security-researcher surface, like CI output. GitHub-remote-only |
| `bin/pr_gate.py` | PR quality gate: classifies a PR **by changed path** (never diff size — a 1-char contract change is the smallest, most dangerous diff), labels `risk:*`/`size:*`/`gate:*`, posts ONE sticky comment (hidden-marker upsert), and arms **native** auto-merge for `docs`/`ci`/`deps` (opt-OUT via `do-not-merge`). Everything above `main()` is a pure, unit-tested seam. ⚠️ polls checks until **terminal** (`axes_are_terminal`), not until `gate_state()` stops being pending — the latter freezes the comment on a transient red; matches the CodeQL **`Analyze`** runs, not the flapping `CodeQL` umbrella |
| `.github/workflows/pr-gate.yaml` | Runs `pr_gate.py` on `pull_request` only (+ a `workflow_dispatch` `backfill` input). ⚠️ NEVER `workflow_run` (fires only from the default branch → untestable on its own PR). After a policy change, run the backfill to re-evaluate already-open PRs. GitHub-remote-only |
| `.github/workflows/pr-reconcile.yaml` | Closes the linked issues + deletes the branches of **merged** PRs. A bot-merged PR does NOT close its issue or delete its branch (bot actions are inert). The **scheduled** run is the real fix (exempt from the GITHUB_TOKEN no-new-runs suppression that also swallows the `closed`-event fast path); no PAT. GitHub-remote-only |
| `bin/check_backlog_ledger.py` | Pre-commit (`check-backlog-ledger`) + CI: fails a branch whose cumulative diff touches `src/` or ci paths but adds no `docs/backlog/<kebab>_YYYYMMDD_HHMMSS.md` ledger with a checkbox. Reuses `pr_gate.classify_risk` **per path** (asking set-membership, not the collapsed most-dangerous class); diffs the **index** vs `merge-base` (a staged ledger counts); no-op off a feature branch. CI needs `fetch-depth: 0`. **Bot PRs are exempt** (`is_bot_author` / `pr_author_login`): a gate demanding a *human-authored* artifact permanently blocks every bot PR that trips it, so the real effect is training people to reach for `--admin`. Keyed on GitHub's own `[bot]` login suffix (no allow-list to rot), read from `github.event.pull_request.user.login` in the event payload — ⚠️ **never `GITHUB_ACTOR`**, which is whoever *triggered* the run, so an actor-keyed exemption dies the moment a human touches the bot's PR. Falls back to the actor only where there is no PR payload, and fails **closed** when the author cannot be resolved; keyed on the author and never the path, so a human branch touching the same files still owes the ledger |
| `bin/db.sh` | Database lifecycle with sub-commands `up\|backup\|restore`: `up` starts services, ensures schema, applies migrations; `backup`/`restore` handle pg_dump/pg_restore. Recipes: `db_up`, `db_backup`, `db_restore` |
| `bin/check_function_length.py` | Pre-commit (`function-length`) + CI + `poe lint`: caps a function or method at **60 lines of CODE**. ⚠️ **Ruff cannot do this** — `PLR0915` counts *statements*, a different metric, so a function can hold 20 statements across 90 lines and pass it. The **docstring is subtracted**, and that is most of the answer: this project mandates NumPy docstrings with Parameters/Returns/Raises, so a raw span measures how well a function is *documented*, not how long it is — measured, 13 functions exceed a raw span where 3 exceed it docstring-excluded, and the pair a raw ceiling flags first are the two best-documented functions in the tree. Shell is measured at the SAME number (two numbers for one rule is the shape that rots), from `name() {` to the column-0 `}` — exact rather than heuristic because `shfmt` guarantees that shape. Fails when audit discovery matches **zero** files and prints the file count on success. `--root <dir>` lets BlueprintX run this very file over its own tree instead of keeping a second copy. Negative control in `tests/unit/test_function_length_gate.py` |
| `bin/check_complexity.sh` | Pre-commit (`check-complexity`) + CI + `poe lint` / `poe check_complexity`: a cyclomatic-complexity ceiling that DIFFERS BY TREE — **1 for `tests/`, 2 for `src/`, 8 for `bin/`**. The `tests/` number is the load-bearing one: a test with a branch tests two paths and the green never says which ran, so complexity 1 is the mechanical form of "each test asserts one behaviour", a rule `tests/CLAUDE.md` states in prose and could never enforce. `bin/` gets 8 because the gates are parsing tools by nature (argv, ruff output, YAML, AST); at 2 they were 74% violating, which is a number nobody pays and therefore a gate nobody keeps. ⚠️ **Does not reimplement mccabe** — ruff already ships it as `C901`, and a hand-rolled counter that treats `assert`/`with` as decision points reports 85% of `tests/` violating where the real figure is 8%: a threshold agreed against one implementation and enforced by another is not the threshold anybody agreed to. Two ruff invocations rather than one config, because `per-file-ignores` can switch a rule OFF for a path but cannot give it a different `max-complexity`. Escape hatch: `# complexity-ok: <reason>` anywhere in the **signature** (not only the `def` line — `ruff format` re-wraps a long signature and pushes a trailing comment onto the closing-paren line, which silently voided a correctly-written hatch until the scan was widened), and the **reason is required**: a bare marker is rejected. Hard-FAILS when ruff cannot be resolved and when discovery matches zero files — a gate reporting its own blindness as OK is the failure mode this repo writes gates to prevent, and this one did exactly that in its first draft (see the file's own header). ⚠️ **Runs ruff with the colour-forcing variables UNSET** (`FORCE_COLOR`, `CLICOLOR_FORCE`), because it parses ruff's rendered `path:line:col:` line and ANSI escapes travel with the digits into an arithmetic expansion — `[0m101[36m: syntax error: operand expected`, a message naming neither ruff nor colour nor the gate. Measured: 6 of 6 tiers red on a clean tree purely from a developer's exported `FORCE_COLOR`. Setting `NO_COLOR=1` is **not** a fix — measured against ruff 0.11.13, the forcing variable wins. Negative controls in `tests/integration/test_bin_scripts.py` |
| `bin/check_docstrings.py` | Pre-commit + `poe check_docstrings`: verifies NumPy docstrings against signatures (param/return types, `Raises`). Parses the combined `a, b : type` form; `Raises` is direct-only. ⚠️ Same zero-discovery guard as `check_provenance.py` — both globs (`src/`, `tests/`) are cwd-relative and both used to report a silent 0 over an empty list (blueprintx#111). Should-fail witness in `tests/integration/test_bin_scripts.py` |
| `bin/check_provenance.py` | Pre-commit (`check-provenance`) + CI: a `src/` module that **calls** `read_table(` (a file/scrape read) must also reference `stamp_provenance`, so a bronze row can never ship without its provenance columns. Keys on the call form — `read_query` (own-DB read) is out of scope; the `tabular_reader.py` seam that *defines* `read_table` is exempt. ⚠️ **Fails when discovery matches zero files and prints the file count on success** — the glob is cwd-relative, so a wrong cwd or a renamed layout used to make it sum an empty list to 0 and exit 0 in silence: success for having checked nothing, the exact failure it exists to catch in someone else's data (blueprintx#111). Should-fail witness in `tests/integration/test_bin_scripts.py` |
| `bin/pin_contract_oracle.py` | Dev helper (manual): extracts a downloaded artifact's **header** and prints it as a `tuple_required` (and optionally writes a header-only `tests/fixtures/<key>__header.csv`), so a contract's columns are *generated from the source bytes*, never transcribed. Header-only → PII-safe. Operates on a local file (no network). See `src/config/CLAUDE.md` |
| `bin/check_layer_imports.py` | Pre-commit (`check-layer-imports`) + CI: deny-by-default import policy per layer, read from the tier's `.layer-policy.yaml`. stdlib and first-party always pass; a third-party module passes only if the file's layer lists it in `allow` **with a written reason**. Two deliberate properties: **scope does not change the verdict** (an import deferred into a function is judged like a top-level one — deferring does not undo the coupling; it only changes the message, because that is the form that used to slip past), and a vendor may be **allowed as a TYPE and denied as an API** (`annotation_only`) — `-> pd.DataFrame` in a signature is the vocabulary the layers share and couples nothing, while `pd.read_sql(...)` is a call every copied file inherits. Complements ruff's `banned-api`, which is a **blocklist** and by construction cannot catch a vendor nobody thought to list. ⚠️ **No longer self-skips.** Modules under `src/` with no policy is a FAILURE: it used to return 0 in silence, so three of the five Python tiers shipped with no import boundary while their CI stayed green. A clean run prints the module count. Also enforces **direction** via per-layer `deny_layers` — `utils/` is imported BY the layers, never the reverse, which involves no vendor and so the allowlist half cannot see it. Direction is deliberately NOT deny-by-default (unlike vendors): the set of layers is small and named in the same file, so an absent entry is a real answer. `src_prefix_depth` lets lib-minimal's `src/<pkg>/_internal/<layer>/` resolve with the same engine |
| `.layer-policy.yaml` *(per tier, not here — and one for python-common's OWN src/)* | The policy the gate reads. Per-tier because the answer differs: `model/` may declare `sqlalchemy` in the ORM tier and nothing in the native one. `first_party_extra: [chassis]` is load-bearing — the shared `utils/` helpers reach their typing engine through the layout shim, so `chassis` is our own code under the *other* layout; without it the gate's first run is 23 false positives |
| `bin/check_comment_language.py` | Pre-commit (`check-comment-language`) + CI + `poe lint`: the **documentation-language boundary** — prose for a *reader* (`README.md`, `docs/`) may be in the project's locale, while code stays English. ⚠️ **What it actually scans is comments and docstrings**, nothing else: Python via `tokenize` + `ast.get_docstring` (exact — a `#` inside a string literal is not a comment), and full-line `#`/`--` comments in `.yaml`/`.yml`/`.sql`/`.sh`/`.toml`/`.cfg`/`.ini`/`.env.example`. Symbol names, commit messages and CI output are covered by the same *convention* but are **not** enforced here — do not read the gate as covering them. Prose was not enough: the rule was breached **twice in one session**, the second time because a sweep grepped only `.py` and left 55 Portuguese comment lines in `src/config/*.yaml`, which then became the convention the next author copied — **a partial sweep leaves a precedent, not just debt**. Hence every commented file type, not merely the one the last violation was in. ⚠️ **The calibration IS the deliverable** (first draft: 19 findings, **18 false**), and each rule is pinned by a named test in `tests/unit/test_comment_language_gate.py`: match locale **function words, never accents** (accents flag every legitimate data label like `Saídas`); redact in a fixed order (double-backtick → single-backtick → quoted → URLs → dotted tokens → ALL-CAPS acronyms, the last worth 5 findings alone via Microsoft `COM`); read comments as **BLOCKS not lines** (a quotation spanning lines otherwise has its language charged to the English comment around it); keep redaction **length-preserving** so a finding names the word's own line. Escape hatch is per **line** (`lang:pt-ok`), never per block. Locale-agnostic: swap `SET_PT_WORDS` to police another language. Fails when audit-mode discovery matches **zero** files, and prints the file count on success — a silent gate is indistinguishable from an absent one |
| `bin/check_docs_sections.py` | Pre-commit (`check-docs-sections`) + CI: every canonical docs page (English **slug** — `index`/`usage`/`examples`/`api/index`/`faq`/`contributing`/`changelog`) exists **and** is registered in `mkdocs.yml` `nav:` (MkDocs builds an unregistered page anyway, so it silently vanishes from nav). Keys on slugs, never localized nav titles. Optional `docs/.docs-skeleton.yaml` overrides `required_pages` and adds per-family required headings (repo-owned labels) |
| `bin/check_contract_drift.py` | Driver for the weekly drift job (service tiers): re-fetches each source in `config/contract_oracles.yaml`, diffs the live header against the shipped contract, writes `contract_drift_report.md`. **Always exits 0** (a reporter, never a gate); self-skips when the registry is empty. ⚠️ **`None` ≠ `[]`** — an unreachable source returns `None` (**not checked**) where a clean comparison returns `[]`, and `main` then prints **SKIPPED** and *never* the all-clear line. Returning `[]` for both was the original defect: the job announced "No contract drift" after comparing nothing — a reporting job reporting its own blindness as OK, the exact failure it exists to catch in someone else's data. Skipped sources stay **out of the report body**, because the report opens an issue and "the source is down" must not arrive looking like "our contract is wrong". The fetch catch is deliberately **broad** (`Exception`): one source may never end the run, and a network error, a malformed registry URL and an undecodable body all mean the same thing to the caller. The timeout is passed **explicitly** per source (`timeout_s:` in the registry) — an unattended scheduled job that hangs reports `cancelled`, indistinguishable from a human pressing cancel. Negative controls in `tests/unit/test_contract_drift.py` |
| `src/config/contract_oracles.yaml` | Drift-oracle registry (`source_key → {url, sep, encoding}`), **empty by default**. Read by `check_contract_drift.py`. Service tiers only |
| `.github/workflows/contract_drift.yaml` | Weekly (+ dispatch) non-gating drift check — runs the driver and opens/updates one **deduplicated** `contract-drift` issue on schema drift; never on PR/push, never fails the run. GitHub-remote-only; service tiers only |
| `bin/ship.sh` | `poe ship` — packages the committed default-branch tree into `dist/<repo-kebab>_YYYYMMDD_HHMMSS.zip` (sourced from `templates/common/bin/`) |
| `bin/run.sh` | Run the project entrypoint (auto-resolves `src/main.py` → `src/controller/main.py` → `src/<pkg>/main.py`); skips reinstall when `.venv` is newer than `pyproject.toml`/`poetry.lock`; Poetry→pip→system fallback chain; sources `lib/bootstrap.sh` + `lib/pip_fallback.sh` and wires the corporate CA |
| `bin/check_unix_filenames.sh` | Pre-commit hook: reject filenames with special characters |
| `bin/lint_deps.sh` | `poe lint` + `lint-deps` hook + CI — **deptry** over `src/`: every imported package must be a DIRECT dependency, never inherited from another package's tree. The rule was prose in `lib-minimal/CLAUDE.md` with no enforcement until blueprintx#238, and its first run found it already broken in the shipped templates: **both DDD tiers imported pandas in five `utils/` modules while declaring it nowhere**, reaching them only through `wwdates` (a business-day calendar). ⚠️ **No system-binary fallback, deliberately** — unlike `lint_shell.sh`/`lint_actions.sh`, whose tools answer the same question wherever they run. deptry maps a module back to its distribution through the metadata of the environment it runs in, so outside the venv the verdict **inverts** rather than degrades: measured on ddd-service-native-db, an outside-venv run produced 9 findings, all false and two mutually contradictory (`python-dotenv` declared-but-unused AND `dotenv` missing), while both genuine defects vanished. Absence is therefore fatal, not a skip. Fails when discovery matches zero files (deptry exits 0 having scanned nothing). ⚠️ **Config lives in each tier's `pyproject.toml`, never a shared `deptry.toml`** — `--config <file>` re-points deptry at that file as the MANIFEST too, so it stops reading the dependency table and relabels every real dep transitive. `bin/` is out of scope: its imports belong to the dev group, and one run cannot hold both profiles. Should-fail witnesses (fires / stays quiet / zero discovery / hard-fails when absent) in `tests/integration/test_bin_scripts.py` |
| `bin/lint_shell.sh` | `poe lint` + `lint-shell` hook — shellcheck + shfmt over `bin/**/*.sh`. Resolves each vendored CLI via `poetry run` (shellcheck-py/shfmt-py), then a system binary, then skips. ⚠️ **Both resolution branches probe `--version`, not just `command -v`** — the latter answers "is there a file by that name on PATH", never "does it run", so a shadowing stub or half-installed binary resolved as present and its startup failure was reported as a lint finding (blueprintx#111; `lint_actions.sh` had the same defect, `lint_docker.sh` did not) |
| `bin/lint_actions.sh` | `poe lint` + `lint-actions` hook + CI — **actionlint** over `.github/workflows/`. `yamllint` validates YAML; `actionlint` validates a *workflow*, and only the second is the question GitHub asks: a workflow GitHub refuses produces **no red check at all** (no label, no comment), so the PR merely looks like it is waiting — a dead gate and a slow gate are indistinguishable from outside. Same resolve-don't-install contract as the other wrappers, with two properties that make it real: it **fails when discovery matches zero files** (actionlint exits 0 with no args, so a broken glob reports success forever) and the `find` expression is **grouped** (`\( -name '*.yaml' -o -name '*.yml' \) -type f`, since `-o` binds looser than the implicit `-a`). CI sets `LINT_ACTIONS_REQUIRED=1` — a graceful skip is right on a contributor's box and placebo in CI. Pins `SHELLCHECK_OPTS` to the house severity so a `run:` block cannot pass one gate and fail the other |
| `bin/check_clean_index.sh` | `pre-push` hook (`check-clean-index`) — refuses to push while the **index** is non-empty. Staged-but-uncommitted work at push time is the fingerprint of a commit that was REJECTED after `git add` ran: the push then ships the *previous* commit and prints success, so every signal is green while the work stays behind. No other layer can see it — CI only sees what was pushed (a commit that does not exist is not a diff) and pre-commit already did its job by rejecting. Guards the **index**, never the dirty tree: unstaged edits while pushing are routine, and a noisy guard gets disabled. Escape hatch: `git push --no-verify` |
| `bin/install_shell_linters.sh` | `poe install_shell_linters` — **optional** system-binary install of shellcheck + shfmt (choco/scoop/brew/apt). The pip dev-deps are primary; this is for boxes whose venv drive blocks the vendored binary |
| `bin/fix_playwright.sh` | Reinstall Playwright browsers — OS-aware `--with-deps` (Linux only); resolves Poetry via the bootstrap lib (`run_poetry`) |
| `bin/test_urls_docstrings.sh` | Pre-commit hook: validate URLs in docstrings (1-week cache) |
| `assets/logo_lorem_ipsum.png` | Placeholder logo copied into new projects |
| `CONTRIBUTING.md` | Contribution guide template |

## What can leave `pyproject.toml` — and what cannot

Audited in blueprintx#233 across all five Python tiers. **The answer is: nothing else.** This
table is here so the question is answered in the repo rather than re-derived by the next person
who looks at a manifest and assumes it must be trimmable.

🔴 **STANDING RULE — every tool added from now on owes this question an ANSWER BEFORE its config
is written.** The default is **out**: a dehydrated manifest is the house style, which is why
`ruff.toml` · `mypy.ini` · `pytest.ini` · `.coveragerc` · `.codespellrc` · `.pydocstyle` ·
`.gitlint` · `.sqlfluff` · `.hadolint.yaml` · `.yamllint` · `.shellcheckrc` ·
`.layer-policy.yaml` · `.cz.toml` all already live outside it. Adding a `[tool.<x>]` block is the
exception and must be justified in this table with the reason it could not move.

⚠️ **And "it cannot move" must be MEASURED, never inferred from the tool's docs or its
resemblance to a tool that could.** `[tool.deptry]` is the worked example: it looks exactly like
`[tool.ruff]` did before `ruff.toml`, it *has* a `--config` flag, and moving it out is silently
catastrophic — the flag re-points the tool at the new file as its **manifest** too. The evidence
is the row below, and the shape of that evidence is the standard: run the tool BOTH ways against
a real scaffolded project and compare the finding counts. A config split that "looks fine" because
the tool still exits 0 is the failure mode — an externalised config that quietly changes what the
tool reads is worse than an inline one, because the manifest gets cleaner while the gate goes
blind.

| Section | Can it move? |
|---|---|
| `[tool.poetry]` | **No** — package identity (name, version, authors, readme, packages) |
| `[tool.poetry.urls]` | **No** — part of the published metadata |
| `[tool.commitizen]` | ~~Yes~~ **Done** — now `.cz.toml`, the only candidate there was |
| `[tool.poetry.dependencies]` | **No** — the resolver's input |
| `[tool.poetry.group.*.dependencies]` | **No** — same |
| `[build-system]` | **No** — required by PEP 518; without it there is no build backend |
| `[tool.deptry]` | **No — and this one has to be MEASURED, not assumed** (blueprintx#238). It looks exactly like `[tool.ruff]` did before it became `ruff.toml`, and it is not: deptry has **no standalone config format**. `--config PATH` is documented as *"Path to the pyproject.toml file to read configuration from"* — it re-points deptry at that file as the **manifest** as well, so the tool stops reading the real dependency table. Measured on ddd-service-orm-db: **27 findings with the config split out, 0 with it inline**, and the 27 include `pandas`, `wwdates` and `yaml` — all declared in the very manifest deptry stopped reading — reported as transitive |

⚠️ **`poetry.toml` is not a second home for `[tool.poetry]`.** The two files sound like a pair
and are not: `poetry.toml` holds **local installer** configuration — `virtualenvs.in-project`,
repository URLs, `http-basic` credentials — and is machine-scoped, which is why this repo's copy
contains only `[virtualenvs] in-project = true`. Project metadata and dependencies must stay in
`pyproject.toml`: that is what PEP 518/621 and Poetry's build backend read. Moving `[tool.poetry]`
there does not slim the manifest, it produces a project that cannot build or resolve.

The manifests are 71–79 lines rather than 300 because the dehydration already happened —
`ruff.toml` · `mypy.ini` · `pytest.ini` · `.coveragerc` · `.codespellrc` · `.pydocstyle` ·
`.gitlint` · `.sqlfluff` · `.hadolint.yaml` · `.yamllint` · `.shellcheckrc` ·
`.layer-policy.yaml` · `.cz.toml` are all already out.

## Editing rules

- **`ruff.toml`**: The active rule sets are `UP E F ANN B SIM I AIR ERA S PD D TID W PL PT`. Two rules are ignored, both because the FORMATTER already settles the question: `D206` (tab-indent + docstring) and **`W191`** (tab-indentation), which would set the linter against `indent-style = "tab"`. ⚠️ Ignoring `W191` is what makes the rest of `W` adoptable — it accounted for 6022 of 6022 findings, so `W` minus `W191` is **zero** today and costs nothing to enable (blueprintx#222). `PLR2004` is ignored in `tests/`: a literal in a test IS the case under test. `max-args = 8`, measured — at pylint's default of 5 the reader and mail seams reported 12 functions, at 8 exactly one remains. Excluded paths include `alembic`, `src/chassis`, and `src/capabilities/example_feature`. Do not remove rule sets without a documented reason. **`E701` is deliberately not ignored** — see the block comment in `ruff.toml`: suppressing it to allow a one-line `if` is dead config because `ruff format` re-expands the statement anyway. **Code style: prefer the expression form** (`return not cond`, a comprehension, `any(...)`, a ternary, dict dispatch) over the multi-line statement form; that is where real concision lives, and the formatter keeps it on one line. General rule: before suppressing a lint rule to enable a style, check whether the **formatter** already forbids it.
- **`.pre-commit-config.yaml`**: Hook versions must stay pinned (`rev:`).
- **`README.md`**: Uses `${VARIABLE}` placeholders — do not replace them with literal values; they are resolved by `envsubst` during scaffolding.
- **`poe_tasks.toml` is the ONLY command list.** There is no second implementation to keep in sync — that requirement is what the migration removed. Two rules replace it: a new task MUST carry a `help =` field (bare `poe` builds the listing from those, so a task without one is invisible), and anything that INVOKES a task from a script, hook or workflow goes through `bash bin/poe_exec.sh <task>`, never a bare `poe`. The shell bootstrap (`bash bin/venv.sh`) is the second and last interface, for hosts where the venv does not exist yet; do not add a third. See `bin/CLAUDE.md`.
- **Never call a bare `poetry`** in a recipe, hook `entry:`, or `bin/*.sh`. Route through `bin/poetry_exec.sh` (`bash bin/poetry_exec.sh …` from a poe task or a `.pre-commit-config.yaml` `entry:`); sourcing scripts use `ensure_poetry` + `run_poetry`; optional linters use `resolve_poetry || skip`. A `pip install --user` Poetry on Windows/Git Bash is reachable only as `python -m poetry`, so a bare `poetry` breaks after `venv` succeeds. See `bin/CLAUDE.md`.
- **Shell-script tests**: the unit gate for a `bin/*.sh` script is `shellcheck` + `bash -n` (run by `bin/lint_shell.sh`); behavioural coverage is a `subprocess` integration test (`tests/integration/test_bin_scripts.py`). New/changed bin scripts must satisfy both.
- **Shipped test guards** (copied into every tier by each scaffold's test-copy step, since the per-tier `cp -r tests/.` does not reach `python-common/tests/`):
  - `tests/CLAUDE.md` — the tests leaf doc, **one source for all six tiers** (blueprintx#124,
    #152). It previously existed only in the two MVC tiers, **byte-identical**, and was absent
    from the other four — so the obvious fix (copy it into the missing four) would have created
    six copies of one file, precisely the drift `check_codespell_sync.sh` exists to police.
    Layout-specific guidance is a **table inside the doc** (mvc / ddd / lib-minimal), not six
    forks. Carries the fixture-as-pinned-oracle rules and the "Proving a claim" section
    (negative controls, mutation granularity, stable digests — ⚠️ never Python's `hash()`,
    randomised per process).
  - `tests/conftest.py` — an **autouse network block** that makes a real network call impossible in any test (swaps the `socket` primitives to raise `NetworkAccessError`, naming the target and the fix). A test that truly must hit the wire opts out with `@pytest.mark.allow_network` (registered in `pytest.ini`). This is the deterministic enforcement of the "mock at the I/O boundary" rule — prose is probabilistic, the guard is not.
  - `tests/unit/test_family_convention_example.py` — a worked example of the **introspective-convention** pattern: a rule every member of a family must follow gets a test that *discovers* the family from `__all__` and asserts the convention on each member, not a doc paragraph. It ships as a self-contained template to copy; delete it if the project has no such family.
- **`src/config/*`**: Edit here, never in the skeleton dirs. `startup.py` is generic (plain `datetime.now()`, no domain calendar); the output directory is **data-driven** from `inputs.yaml` (`daily_infos_base_path` + `daily_infos_dated`) — do not hard-code paths or re-add a `folder` key. Filename templates use **named** placeholders, not positional `{}` + comments.
- **`optional/webhook/`**: `SlackNotifier.send` is an intentional contribution stub (raises `NotImplementedError`). The `WebhookNotifier` port is the contract `startup.py` depends on — keep adapters structurally conformant so swapping platforms never touches `startup.py`. Internal imports use the canonical `chassis.webhook` prefix; the MVC scaffolds rewrite it to `utils.webhook` on copy.
- All shell scripts must source `bin/lib/common.sh` and use `print_status` for status output — never bare `echo`/`printf` for status messages.
- **`bin/lib/bootstrap.sh`**: a sourced lib — define-only, no work on source. Callers run `bootstrap_init` before the other helpers. `ensure_python_version` must keep the pyenv-preferred / system-Python fallback (some corporate hosts forbid installing pyenv). `wire_corporate_ca` must stay a no-op when `bin/corporate_ca.pem` is absent, so non-corporate setups keep full TLS verification.
- **`bin/get_corporate_ca.sh`**: the only place TLS verification is intentionally disabled (to capture a proxy CA). Keep it manual (`poe get_corporate_ca`) — never auto-run it from `venv.sh`. The generated `bin/corporate_ca.pem` is git-ignored and must never be committed.
