# Data file read by check_comment_budget.py — one substring per line.
#
# A line here is a literal substring; any comment line CONTAINING it is
# machine-read configuration wearing comment syntax, not prose being
# budgeted. It never counts toward a run, is never flagged as a banner, and
# is never stripped — it BREAKS a run instead of extending it (blueprintx#303).
#
# Lines starting with "#" are section headers for this file only; they are
# never searched for. Blank lines are ignored.
#
# 🔴 ANTICIPATORY, NOT A CENSUS. Add every pragma form the toolchain in
# templates/python-common/ can emit, even ones with zero occurrences today —
# codespell:ignore has zero inline uses (codespell is configured through
# .codespellrc instead) and belongs here anyway, because the first person who
# legitimately needs one must not be the one who finds out the gate has never
# seen it.

# ruff
noqa
ruff:

# mypy
type: ignore
mypy:

# pytest / coverage
pragma: no cover

# shellcheck
shellcheck disable

# codespell
codespell:ignore

# this repo's own gates
complexity-ok:
lang:pt-ok
layer-policy
dtype-ok:
rmw-ok:
gate-change-ok:

# deptry (per_rule_ignores / DEP00N codes)
DEP00

# hadolint
hadolint ignore

# yamllint
yamllint disable

# sqlfluff
sqlfluff:
-- noqa

# secrets scanners
gitleaks:allow
nosec
trivy:ignore
checkov:skip

# formatters
fmt: off
fmt: skip
prettier-ignore

# TypeScript / JS toolchain
@ts-expect-error
@ts-ignore
eslint-disable
stylelint-disable

# pylint
pylint: disable

# generic "do not analyze this" markers
NOSONAR
depcheck-ignore

# dependency bots
renovate:
dependabot

# ponytail
ponytail:

# structural exemptions (blueprintx#303): SPDX headers and generated-file
# banners are not the essay/decorative-banner defect this gate polices.
SPDX-License-Identifier
DO NOT EDIT
@generated
autogenerated
auto-generated

# ⚠️ blueprintx#466/#479: the tests/CLAUDE.md test-section-banner convention
# ("# --------------------------" / "# <section>" / "# --------------------------")
# is deliberately NOT listed here. Every entry in this file is a PRAGMA, and
# is_pragma_line/line_breaks_run makes a pragma BREAK the current comment run
# — that is correct for a QA suppression, but it means a "banner is a pragma"
# entry lets one dash line split an oversized block into two under-ceiling
# halves (reproduced: 44 lines + one dash line + 44 lines passed clean). The
# banner exemption is handled structurally in check_comment_budget.py itself
# (STR_TEST_SECTION_RULE / banner_findings), which counts the two dash lines
# toward the block's length but never lets them end the run — do not "fix"
# this by re-adding the string here.
