node_modules/
dist/

# Git-diff offline-sync exports (present only when scaffolded without GitHub)
/git_diffs/

# Repo→LLM context dump generated by `npm run context:export`
/repo_context.txt

# Scaffoldable lessons — local working copy only; the canonical, cross-project
# store lives globally in ~/.claude/memory/lessons/. Never committed here.
docs/blueprintx-lessons.md

.env
.env.local
*.log
.DS_Store
coverage/
.nyc_output/

# Playwright — keep test specs and committed visual baselines (*-snapshots/),
# ignore generated reports and run artifacts.
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/
