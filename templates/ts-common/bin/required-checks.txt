# Status-check names bin/enable_repo_rules.sh makes merge-blocking on the default branch.
# One name per line, verbatim as GitHub reports it; blank lines and `#` comments ignored.
#
# Add a name only after confirming a real PR emits it:
#   gh api repos/:owner/:repo/commits/<pr-head-sha>/check-runs --jq '.check_runs[].name' | sort -u
#
# A name nothing emits blocks every PR forever — GitHub cannot tell "has not reported yet"
# from "will never report". Deleting a line is always safe; adding an unverified one is not.

# review-threads.yml — `on: pull_request` with no filter, the one workflow in this family that
# reports on every PR regardless of base branch.
Review threads answered

# ⚠️ NOT listed, and the reason is worth keeping (blueprintx#564): `webpack` (build.yml),
# `eslint`/`stylelint` (lint.yml), `jest` (test.yml), `type-check` (type-check.yml) and, in the
# ts-lib skeleton, `build` (docs.yml) plus `pack-smoke`/`verdaccio-rehearsal` (pack-smoke.yml)
# all trigger on `pull_request: branches: [main]`. A base-branch filter means the check is simply
# absent on a PR that targets anything else — a stacked PR onto a feature branch, for one — and
# an absent required check reads as permanently pending, not as passed. Widen those triggers
# first if you want them binding; requiring them as they stand is the deadlock this file warns
# about.
