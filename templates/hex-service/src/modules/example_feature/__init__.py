"""Example feature module to illustrate per-feature layering (domain/services/infrastructure).

Replace `example_feature` with your bounded context or feature name and extend the
contents under `domain`, `services`, and `infrastructure` accordingly.
"""
