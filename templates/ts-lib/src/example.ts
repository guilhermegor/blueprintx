/**
 * Example pure function — replace with your library's real public API.
 *
 * @param name - Who to greet.
 * @returns A greeting string.
 */
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
