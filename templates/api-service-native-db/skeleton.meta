language=python
display_name=API Service (Native DB)
description=Hexagonal HTTP API service (FastAPI transport layer) with native DB drivers (psycopg2, sqlite3, mysql-connector, etc.)
scaffold=bin/scaffold/python_api_service.sh
