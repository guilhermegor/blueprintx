[project]
name = "${PROJECT_NAME}"
version = "0.1.0"
description = "Python library generated with blueprintx (lib-minimal)."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest",
]

[tool.uv]
# Customize uv settings if needed
