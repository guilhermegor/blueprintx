# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Virtual environments
.venv/
venv/
env/

# uv
.uv/

# Distribution / packaging
# dist/ holds build artifacts and `poe ship` zips — ignore its contents but keep
# the tracked dist/.keep so the directory ships with the scaffold.
build/
dist/*
!dist/.keep
*.egg-info/

# IDE / editor
# Ship the curated VS Code settings + tasks; ignore everything else under .vscode/.
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/extensions.json
.idea/
*.swp

# Test / coverage
# coverage.svg is tracked (the README badge references it); coverage.xml is the
# transient genbadge input and is regenerated on every run.
.coverage
coverage.xml
htmlcov/

# MkDocs build output (generated by `mkdocs build`)
site/

# pre-commit check-urls hook cache
.url_check_cache/

# Git-diff offline-sync exports (present only when scaffolded without GitHub)
/git_diffs/

# Repo→LLM context dump generated by `poe export_context`
/repo_context.txt

# Corporate CA (generated locally by `poe get_corporate_ca` — never committed)
bin/corporate_ca.pem
# The UNION bundle wire_corporate_ca derives from it (certifi + host bundle + corporate CA)
bin/ca_bundle.pem

# Claude Code machine-local settings + its mid-save temp siblings. The
# `.tmp.<pid>.<hash>` file is written briefly during a save; a `git add -A` that
# races that window would otherwise sweep it into a commit.
.claude/settings.local.json
.claude/settings.local.json.tmp.*

# Scaffoldable lessons — local working copies only; the canonical, cross-project
# stores live globally in ~/.claude/memory/lessons/ (templates) and
# ~/.claude/memory/lessons-dotfiles/ (the dotfiles-dev Claude toolchain). Never committed here.
docs/blueprintx-lessons.md
docs/dotfiles-dev-lessons.md

# Environment files — ignore real env files, keep .env.example tracked
.env
.env.local
.env.dev
.env.development
.env.test
.env.staging
.env.prod
.env.production

# Local dev SQLite artifact (config.connection_db default DB_PATH=./data/app.db when
# DB_BACKEND=sqlite). The runtime DB is never committed; .env.example documents DB_PATH.
data/app.db
