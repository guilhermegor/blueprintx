# **Examples**

Task-oriented, self-contained snippets for common work in this service. Each recipe stands
alone — copy it and adjust.

> **See also:** [Usage](usage.md) for the basics · [Architecture](architecture.md) for where
> each piece belongs.

---

## Recipe: add a new model entity / repository

Describe the data you need, then show the smallest example following the tier's layer rules
(data access in the model/repository layer, never in the view or controller).

```python
# Replace with a real example: load a table into a typed DataFrame via apply_dtypes,
# or read/write through the repository, per this tier's conventions.
```

## Recipe: add a new output/view

Add one `##` section per recipe. Comments should explain **why**, not just what.

```python
# Replace with a second illustrative example (e.g. render a report, emit JSON).
```
